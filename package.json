{
	"name": "project-air-to-surface",
	"devDependencies": {
		"@types/node": "^24.10.1",
		"vite": "^7.2.4"
	},
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "bun run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"type": "module",
	"dependencies": {
		"kaplay": "4000.0.0-alpha.22"
	}
}